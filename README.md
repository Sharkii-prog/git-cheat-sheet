# 📚 Git Commands Cheat Sheet (Шпаргалка)

![Git](https://img.shields.io/badge/Git-CheatSheet-red)
![Version](https://img.shields.io/badge/version-2024-blue)
![Level](https://img.shields.io/badge/level-all-green)

> 📌 **Сохраните эту страницу в закладки!** Все основные команды Git в одном месте.

---

## 📋 Оглавление

- [🔧 Настройка и конфигурация](#настройка-и-конфигурация)
- [📁 Создание и клонирование репозитория](#создание-и-клонирование-репозитория)
- [📝 Staging изменения и коммиты](#staging-изменения-и-коммиты)
- [🌿 Ветвление и слияние](#ветвление-и-слияние)
- [🌐 Удалённые репозитории](#удалённые-репозитории)
- [🔍 Просмотр истории и изменений](#просмотр-истории-и-изменений)
- [↩️ Отмена изменений](#отмена-изменений)
- [🧹 Очистка и оптимизация](#очистка-и-оптимизация)
- [🔖 Теги и релизы](#теги-и-релизы)
- [🛡️ Безопасность и секреты](#безопасность-и-секреты)
- [📊 Визуализация графа](#визуализация-графа)
- [🎯 Популярные workflow](#популярные-workflow)
- [📈 Статусы файлов в Git](#статусы-файлов-в-git)
- [⌨️ Горячие клавиши и алиасы](#горячие-клавиши-и-алиасы)
- [🚨 Опасные команды](#опасные-команды)
- [🔗 Полезные ссылки](#полезные-ссылки)

---

## 🔧 Настройка и конфигурация

| Команда | Описание |
|---------|----------|
| `git config --global user.name "Имя"` | Установить имя пользователя |
| `git config --global user.email "email@example.com"` | Установить email пользователя |
| `git config --global core.editor "code --wait"` | Установить редактор по умолчанию (VS Code) |
| `git config --list` | Показать все настройки |
| `git config user.name` | Показать текущее имя |
| `git config --global --unset user.name` | Удалить настройку |
| `git config --global alias.st status` | Создать алиас (st = status) |
| `git config --global color.ui true` | Включить цветовое оформление |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 📁 Создание и клонирование репозитория

| Команда | Описание |
|---------|----------|
| `git init` | Инициализировать новый репозиторий |
| `git clone <url>` | Клонировать удалённый репозиторий |
| `git clone <url> <folder-name>` | Клонировать в папку с именем |
| `git clone --depth 1 <url>` | Клонировать только последний коммит |
| `git clone --branch <name> <url>` | Клонировать конкретную ветку |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 📝 Staging изменения и коммиты

| Команда | Описание |
|---------|----------|
| `git status` | Показать статус изменений |
| `git add <file>` | Добавить файл в staged |
| `git add .` | Добавить все изменённые файлы |
| `git add -A` | Добавить все (включая удалённые) |
| `git add -p` | Добавить изменения по частям (interactive) |
| `git commit -m "Сообщение"` | Сделать коммит с сообщением |
| `git commit -am "Сообщение"` | Add + Commit для отслеживаемых файлов |
| `git commit --amend` | Изменить последний коммит |
| `git commit --amend --no-edit` | Изменить коммит без смены сообщения |
| `git reset HEAD <file>` | Убрать файл из staged |
| `git restore --staged <file>` | Альтернатива reset для staged |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🌿 Ветвление и слияние

| Команда | Описание |
|---------|----------|
| `git branch` | Показать все локальные ветки |
| `git branch -a` | Показать все ветки (локальные + удалённые) |
| `git branch <name>` | Создать новую ветку |
| `git branch -d <name>` | Удалить ветку (безопасно) |
| `git branch -D <name>` | Удалить ветку (принудительно) |
| `git checkout <name>` | Переключиться на ветку |
| `git switch <name>` | Переключиться на ветку (новая команда) |
| `git switch -c <name>` | Создать и переключиться на ветку |
| `git checkout -b <name>` | Создать и переключиться (старый синтаксис) |
| `git merge <branch>` | Слить ветку в текущую |
| `git merge --no-ff <branch>` | Слить с сохранением истории ветки |
| `git rebase <branch>` | Перебазировать текущую ветку на другую |
| `git rebase -i HEAD~3` | Интерактивная перебазировка последних 3 коммитов |
| `git cherry-pick <commit>` | Применить конкретный коммит к текущей ветке |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🌐 Удалённые репозитории

| Команда | Описание |
|---------|----------|
| `git remote` | Показать удалённые репозитории |
| `git remote -v` | Показать URL удалённых репозиториев |
| `git remote add origin <url>` | Добавить удалённый репозиторий |
| `git remote remove origin` | Удалить удалённый репозиторий |
| `git remote rename old new` | Переименовать удалённый репозиторий |
| `git fetch` | Получить изменения с сервера (без слияния) |
| `git fetch --all` | Получить со всех удалённых репозиториев |
| `git pull` | Fetch + Merge (получить и слить) |
| `git pull --rebase` | Fetch + Rebase (получить и перебазировать) |
| `git push` | Отправить изменения на сервер |
| `git push -u origin main` | Отправить и установить upstream |
| `git push --force` | Принудительный пуш (опасно!) |
| `git push --force-with-lease` | Безопасный принудительный пуш |
| `git push --tags` | Отправить все теги |
| `git push origin --delete <branch>` | Удалить ветку на сервере |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🔍 Просмотр истории и изменений

| Команда | Описание |
|---------|----------|
| `git log` | Показать историю коммитов |
| `git log --oneline` | Краткий формат (1 строка на коммит) |
| `git log --graph` | Показать граф веток |
| `git log --oneline --graph --all` | Полный граф всех веток |
| `git log -n 5` | Показать последние 5 коммитов |
| `git log --since="2 weeks ago"` | Коммиты за последние 2 недели |
| `git log --author="Name"` | Коммиты конкретного автора |
| `git show <commit>` | Показать детали коммита |
| `git diff` | Показать изменения в working directory |
| `git diff --staged` | Показать изменения в staged |
| `git diff <branch1> <branch2>` | Сравнить две ветки |
| `git diff HEAD~1 HEAD` | Сравнить последний коммит с предыдущим |
| `git blame <file>` | Показать, кто и когда изменил каждую строку |
| `git shortlog -sn` | Статистика коммитов по авторам |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## ↩️ Отмена изменений

| Команда | Описание |
|---------|----------|
| `git restore <file>` | Отменить изменения в файле (working directory) |
| `git restore --staged <file>` | Убрать файл из staged |
| `git checkout -- <file>` | Старый синтаксис отмены изменений |
| `git reset --soft HEAD~1` | Отменить коммит, сохранить изменения в staged |
| `git reset --mixed HEAD~1` | Отменить коммит, сохранить изменения в working directory |
| `git reset --hard HEAD~1` | Отменить коммит и все изменения (опасно!) |
| `git revert <commit>` | Создать новый коммит, отменяющий указанный (безопасно) |
| `git clean -n` | Показать, какие файлы будут удалены |
| `git clean -f` | Удалить не tracked файлы |
| `git clean -fd` | Удалить не tracked файлы и папки |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🧹 Очистка и оптимизация

| Команда | Описание |
|---------|----------|
| `git gc` | Запустить сборку мусора (оптимизация) |
| `git gc --aggressive` | Агрессивная оптимизация |
| `git fsck` | Проверить целостность репозитория |
| `git prune` | Удалить недоступные объекты |
| `git reflog` | Показать историю всех действий (даже после reset) |
| `git fsck --lost-found` | Найти потерянные коммиты |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🔖 Теги и релизы

| Команда | Описание |
|---------|----------|
| `git tag` | Показать все теги |
| `git tag -l "v1.*"` | Показать теги по маске |
| `git tag v1.0.0` | Создать легковесный тег |
| `git tag -a v1.0.0 -m "Версия 1.0.0"` | Создать аннотированный тег |
| `git tag -d v1.0.0` | Удалить тег локально |
| `git push origin v1.0.0` | Отправить тег на сервер |
| `git push --tags` | Отправить все теги |
| `git push origin --delete tag v1.0.0` | Удалить тег на сервере |
| `git checkout v1.0.0` | Переключиться на тег (detached HEAD) |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🛡️ Безопасность и секреты

| Команда | Описание |
|---------|----------|
| `git update-index --assume-unchanged <file>` | Игнорировать изменения в файле |
| `git update-index --no-assume-unchanged <file>` | Вернуть отслеживание файла |
| `git rm --cached <file>` | Удалить файл из репозитория (оставить локально) |
| `git filter-branch --force` | Переписать историю (удалить секреты) |
| `git secrets --install` | Установить проверку на секреты (требует плагин) |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 📊 Визуализация графа

| Команда | Описание |
|---------|----------|
| `git log --oneline --graph --all --decorate` | Красивый граф всех веток |
| `gitk` | Графический просмотрщик истории (GUI) |
| `git log --pretty=format:"%h - %an, %ar : %s"` | Кастомный формат лога |

**Пример алиаса для красивого лога:**
```bash
git config --global alias.lg "log --oneline --graph --all --decorate"
```
После этого можно использовать: `git lg`

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🎯 Популярные Workflow

### 🔁 Ежедневная работа
```bash
# Начать работу над новой фичей
git switch -c feature/new-feature

# В конце дня
git add .
git commit -m "feat: добавить новую функцию"
git push -u origin feature/new-feature

# Утром следующего дня
git fetch
git rebase origin/main
```

### 🔄 Синхронизация с main
```bash
git switch main
git pull origin main
git switch feature/new-feature
git rebase main
```

### 🐛 Хотфикс в продакшен
```bash
git switch main
git switch -c hotfix/critical-bug
# Исправить баг
git commit -am "fix: критическое исправление"
git push -u origin hotfix/critical-bug
# Создать Merge Request на GitFlic
```

### 🏷️ Подготовка релиза
```bash
git switch main
git pull origin main
git tag -a v1.2.0 -m "Релиз версии 1.2.0"
git push origin v1.2.0
```

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 📈 Статусы файлов в Git

| Статус | Значение | Команда |
|--------|----------|---------|
| 🟢 **Untracked** | Файл не отслеживается | `git add <file>` |
| 🟡 **Staged** | Файл готов к коммиту | `git commit` |
| 🔵 **Committed** | Файл закоммичен | — |
| 🔴 **Modified** | Файл изменён после коммита | `git status` |
| ⚫ **Deleted** | Файл удалён | `git rm <file>` |

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## ⌨️ Горячие клавиши и алиасы

Добавьте в `~/.gitconfig` для ускорения работы:

```ini
[alias]
    st = status
    co = checkout
    br = branch
    ci = commit
    lg = log --oneline --graph --all --decorate
    last = log -1 HEAD
    unstage = reset HEAD --
    who = shortlog -sn --
```

После этого:
- `git st` вместо `git status`
- `git lg` вместо `git log --oneline --graph --all --decorate`
- `git who` для статистики авторов

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🚨 Опасные команды (использовать с осторожностью!)

| Команда | Риск | Когда использовать |
|---------|------|-------------------|
| `git reset --hard` | 🔴 Потеря всех изменений | Только если уверен на 100% |
| `git push --force` | 🔴 Перезапись истории на сервере | Только в личных ветках |
| `git clean -fd` | 🔴 Удаление всех не tracked файлов | После проверки `git clean -n` |
| `git filter-branch` | 🔴 Перепись всей истории | Для удаления секретов из истории |

> ✅ **Безопасная альтернатива:** `git revert` вместо `git reset` для публичных веток

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 🔗 Полезные ссылки

- 📖 [Официальная документация Git](https://git-scm.com/doc)
- 🎮 [Интерактивный тренажёр](https://learngitbranching.js.org/?locale=ru_RU)
- 📄 [Шпаргалка от GitHub](https://education.github.com/git-cheat-sheet-education.pdf)
- 🎬 [Видео-курсы по Git](https://www.youtube.com/results?search_query=git+tutorial)
- 💬 [Сообщество GitFlic](https://gitflic.ru/)

⬆️ [Вернуться к оглавлению](#-оглавление)

---

## 📄 Лицензия

Эта шпаргалка доступна для свободного использования и распространения.

---

*👨‍💻 Автор: [nonet2t](https://gitflic.ru/user/nonet2t)*  
*📅 Последнее обновление: 2024*  
*⭐ Если полезно — поставь звезду репозиторию!*
